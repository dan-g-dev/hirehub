# Only needed if you run the frontend against a backend that is NOT
# proxied by Vite (see vite.config.ts) — e.g. a deployed backend URL.
# When set, update src/services/api.ts to read it via import.meta.env.
# VITE_API_URL=https://api.hirehub.et
